# 1. Sorting

```java
String[] str = {"a", "A", "b"}; 
Arrays.sort(str, String.CASE_INSENSITIVE_ORDER); //ignore upper or lowercase

Arrays.sort(str, Collections.reverseOrder()); //reverse order
```

